# 03-high-dependency-slowdown

File `data/jtl-summary.csv` được sinh từ raw JTL của kịch bản `03_high_dependency_slowdown_mixed_0900_staged_tst`.

**Chỉ các dòng `MEASURE_` được dùng để tính kết quả.**

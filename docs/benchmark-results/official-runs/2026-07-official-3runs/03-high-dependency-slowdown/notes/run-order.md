# Thứ tự chạy xác định từ Grafana

**Thứ tự chạy:** Random → Adaptive → Round Robin

Thứ tự này được xác định từ panel `Routing Selection Rate by Decision Reason`, không dựa vào thứ tự dòng trong CSV.
